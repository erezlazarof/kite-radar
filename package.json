{
  "name": "kite-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler pages dev public --port 3300"
  }
}
