# מסירה — רדאר קייט

## 🟢 האתר חי: https://yeshruach.pages.dev

נפרס 20/8/2026. Cloudflare Pages ← GitHub (`erezlazarof/kite-radar`, ענף `master`).
כל `git push` מעדכן את האתר תוך ~30 שניות. אין פקודת deploy.
אומת בפריסה: 26 כרטיסים · 24 עם מדידה חיה · **חמישה מקורות fresh מהענן** ·
**ה-Service Worker נרשם והשתלט (`swController: true`) — החוב הפתוח משלב 5 נסגר.**

---

## ⚠️ הצעד הבא: דיוק ואמון — זו המשימה המרכזית

ארז העלה את זה ב-20/8 וזה הדבר החשוב ביותר בפרויקט כרגע:

> "באילת כתוב 13 בגדול אבל המדידה האחרונה הראתה 16. זה חייב להיות אמין,
> אחרת זה חסר ערך. הייתי רוצה לכתוב מה המקור לנתון וזמן מדידה אחרון,
> לתת לאנשים להגיש אמון באתר."

**האבחון, מאומת בנתונים חיים (20/8, אילת ריף רף):**

| | |
|---|---|
| המספר הגדול בכרטיס | **13** = תחזית ECMWF, ממוצע הבלוק הרצוף הטוב ביותר (12:00–13:00 = 13.4) |
| "נמדד עכשיו" | **16–19** = האנמומטר של Surf Center, ברגע זה |

תחזית ECMWF לאילת היום: 13.7 / 13.2 / 12.5 / 11.8 / 11.7 / 11.9 לאורך 12:00–18:00.
המדידה בפועל 16–19. **המודל פשוט מחסיר 4–6 קשר באילת היום.**

**זה לא באג — זו בעיית עיצוב, והיא חמורה יותר מבאג:**
1. שני המספרים נכונים, אבל הם **דברים שונים** (תחזית לחלון מול מדידה עכשיו), והכרטיס לא אומר את זה.
2. **המספר הפחות אמין מוצג הכי גדול.** כשיש אנמומטר על החוף עצמו, הוא האמת — והתחזית היא הניחוש.
3. `obs_disagrees` לא נורה כאן: הוא משווה מדידה לתחזית **בשעת המדידה** (16 מול 12.5 = 3.5 קשר), מתחת לסף 5. כלומר המשתמש לא מקבל שום רמז.

**כיווני פתרון לשקול (לא הוכרעו):**
- כשיש מדידה טרייה — להפוך אותה למספר הראשי, והתחזית יורדת ל"מאוחר יותר היום".
- להצמיד לכל מספר תווית מקור וזמן: "תחזית ECMWF · חלון 12:00–14:00" מול "נמדד · Surf Center · לפני דקה".
- להוריד את סף `OBS_DISAGREE_KT` כשהמקור הוא `club` (המדידה על החוף עצמו — פער של 3 קשר כבר משמעותי), ולהשאיר 5 לתחנה אזורית.
- **פאנל "מאיפה המספר"** בכל כרטיס: מקור, מרחק, זמן מדידה אחרון, ומתי נמשכה התחזית. זה בדיוק מה שארז ביקש ומה שבונה אמון.

⚠️ להיזהר מחוק ברזל 5: מדידה לא מחליפה פסק דין ולא משדרגת אותו. שינוי כאן חייב בדיקות חדשות ב-`test/verdict.test.js`.

---

## אחרי זה: שלב 7 — טלגרם

הקוד כתוב, 45 בדיקות עוברות. `worker/README.md`. חסר:
`npm install -g wrangler` · טוקן בוט מ-BotFather · `wrangler secret` · `wrangler deploy`.
⚠️ Claude חסום מ-`wrangler deploy` — ארז מריץ.

---


**נכון ל-20/8/2026.** 254 בדיקות עוברות. **נדחף ופרוס.**

---

## מה זה

אתר ציבורי חינמי בעברית: תחזית רוח **ומדידה חיה** לספוטי הקייטסרפינג בישראל, עם פסק דין "ללכת לים?" לכל חוף. ארז לזרוף וחבריו גולשי קייט.

התוכנית המלאה נשמרת מחוץ לריפו.
הכללים לסשן: `CLAUDE.md` בתיקייה הזו — **לקרוא לפני נגיעה בקוד.**

---

## הרצה — 30 שניות

**השרת כבר רץ** תחת pm2 בשם `kite-radar-dev` על פורט 3300, ומשרת את הריפו הראשי.
הוא **לא** רץ בתוך סשן: סשן שמחזיק תהליך חי נראה כאילו הוא עדיין עובד, ותהליך יתום
אחרי סגירת סשן תופס את הפורט וגורם ל-EADDRINUSE בסשן הבא. זה כבר קרה פעמיים.

```
pm2 list | Select-String kite       # מצב
pm2 logs kite-radar-dev --lines 50  # לוגים
pm2 restart kite-radar-dev          # אחרי שינוי בקוד השרת
```

⛔ **לא להריץ `node dev-server.js` בתוך סשן.** אם אתה בענף אחר (worktree) והשרת של
master לא מתאים — הרם משלך על פורט אחר: `PORT=3301 node dev-server.js`.

<http://127.0.0.1:3300> — האתר, כולל `/api/obs` החי.
`dev-server.js` מריץ את **הפונקציה האמיתית** מ-`functions/api/obs.js`, בלי wrangler ובלי פריסה. מה שנבדק כאן הוא מה שירוץ בענן.

| כתובת | מה |
|---|---|
| `/` | האפליקציה |
| `/dev/states.html` | **גלריית מצבים** — מצבי פסק הדין על נתונים סינתטיים. לפתוח אחרי כל שינוי ויזואלי; היא חשפה את רוב הבאגים. `?only=dials` = פס החוגות בלבד · `?dark` = כהה |
| `/dev/map.html` | מפת הרג'יסטר. מחוללת ב-`node public/dev/build-map.js public/dev/map.html` |

**אם 3300 תפוס — לבדוק לפני שהורגים.** ברוב המקרים זה pm2 עושה את עבודתו:
`pm2 list | Select-String kite`. `Stop-Process` על ה-PID הזה מפיל את שרת ה-master
ופותח בעיה במקום לסגור אחת. תהליך זר מזוהה לפי PID שאינו מופיע ב-`pm2 list`.

`npm test` — 254 בדיקות. **להריץ לפני כל push.**

---

## מצב השלבים

| # | שלב | מצב |
|---|---|---|
| 0 | שלד ופריסה | ✅ |
| 1 | כרטיסי פסק דין | ✅ |
| 1b | מערכת עיצוב v2 | ✅ מטאוגרם בכרטיס, מצפן, פלטת ים, חמישה מצבים נבדלים מבנית |
| 1c | ניווט ורג'יסטר | ✅ **26 ספוטים**, צ'יפי אזור, קיבוץ עם כותרות דביקות, גיליון הגדרות |
| 2 | פאנל ומטאוגרם 72 שעות | ✅ עם סקראבר |
| 3 | השוואת מודלים | ✅ |
| 4 | מדידה חיה | ✅ |
| 5 | PWA | ✅ **⚠️ הפעלת ה-SW לא אומתה חיה** — ראה למטה |
| 6 | הוסף ספוט משלך | ✅ שלוש שכבות, אפס מסד נתונים |
| 7 | טלגרם | 🔄 קוד כתוב, 45 בדיקות · **ממתין לטוקן בוט ולפריסה ידנית של ארז** |
| 8 | עומק ימי (גלים) | ⬜ |

---

## הצעד הבא המיידי

**להעלות לרשת.** הקוד גמור; מה שחסר הוא חשבון, ריפו ופריסה — ראה הסעיף הבא.
אחרי זה: שלב 8 (עומק ימי), או בוט הטלגרם לפי `worker/README.md`.

---

## שלב 6 — איך זה בנוי

שלוש שכבות, בלי מסד נתונים ובלי שרת:

| שכבה | איפה | מה |
|---|---|---|
| מקומי | `localStorage['kite.userSpots']` | הטופס ב-`#addspot`. נ.צ. בהדבקה או מיקום נוכחי, וחוגה לכיוון החוף |
| שיתוף | `#addspot=<base64url>` | `encodeShare` / `decodeShare`. **הקישור לעולם לא מוסיף בשקט** — הוא פותח מסך אישור עם באנר אזהרה |
| קידום | issue מוכן בגיטהאב | `issueBody()` — JSON + **צ'ק-ליסט בטיחותי**. `GITHUB` ב-`config.js`; `owner: null` מחליף את הכפתור בהעתקה ללוח |

**קבצים:** `public/js/userspots.js` (טהור, נבדק ב-node) · `public/js/ui/dial.js` (טהור, מחזיר SVG) · `public/js/ui/addspot.js` (DOM בלבד).

**שלוש החלטות שכדאי לדעת עליהן לפני ששוברים אותן:**

1. **אין מפה.** מפה = ספק אריחים חיצוני = תלות ברשת בדיוק במקום שבו האפליקציה מבטיחה לעבוד בלי רשת. במקום זה: הדבקת נ.צ. מגוגל מפות (כולל קישור מלא ו-DMS), או `geolocation`.
2. **החוגה מציירת תוצאה, לא מעלות.** אדם שהיה בחוף יודע לומר "מזרחית כאן מסוכנת" הרבה לפני "הניצב הוא 278". הרצועה האדומה היא בדיוק מה שהמנוע קורא לו אופשור, ושתי בדיקות נפרדות אוכפות את זה:
   * `dialSentence` מול `directionClass` — **360 ניצבים על 120 כיווני רוח.** זו המתמטיקה.
   * **פירוק ה-SVG שיוצא מ-`renderDial`** — איזו קשת נושאת את `--cmp-danger` ואיזו את `--cmp-safe`, ומה המנוע אומר על כל מעלה בתוכן. זו הבדיקה שבאמת מגנה: בלעדיה החלפת שני ארגומנטי הצבע צבעה את קשת הסכנה בירוק **וכל 226 הבדיקות עברו.**
3. **`source: 'user'` נכפה מחדש בכל קריאה מ-localStorage.** יש בדיקה שמזייפת `source: 'core'` ומוודאת שהיא לא עוברת.

**ספוט משתמש מקבל מדידה חיה** אם יש תחנת שמ"ט בטווח 12 ק"מ — טבלת התחנות נגזרת מהרג'יסטר עצמו (`stationsFromRegistry`), אין קובץ תחנות נפרד.

---

## מה שארז צריך לעשות בעצמו

**אני חסום מלהריץ `wrangler deploy`.** הארכיטקטורה תוכננה כך שהפקודה הזו נדרשת פעם אחת ואז כמעט לעולם לא.

1. **github.com → New repository → `kite-radar` → Public → בלי README.**
2. `git push -u origin main` — נפתח חלון התחברות פעם אחת בחיים.
3. **Cloudflare → Workers & Pages → Create → Pages → Connect to Git → `kite-radar`.**
   Framework preset **None** · Build command **ריק** · Output directory **`public`**.
   ⛔ **לא להפעיל Cloudflare Access.** ב-`erez-pm-dashboard` הוא כן מופעל ונעול למייל שלו — כאן ההפך. טעות שם מייצרת אתר שרק הוא יכול לפתוח, **ונראה שהוא עובד**.
4. טלגרם — לפי `worker/README.md`.

מכאן והלאה כל עדכון הוא `git add . ; git commit ; git push` וקלאודפלייר מעלה תוך ~30 שניות. **את זה אפשר לעשות לבד.**

---

## חובות פתוחים שדורשים הכרעה של ארז

| # | מה | מצב |
|---|---|---|
| ~~1~~ | ~~אילת — איזה חוף?~~ **הוכרע 19/8/26: ארז גולש בריף רף.** הסקר צדק — ריף רף הוא חוף הקייט, והחוף הצפוני הוא חוף רחצה ומרינה. `eilat-reef-raf` סומן כמאושר-בעלים ודירוג הוודאות עלה ל-high | ✅ |
| ~~2~~ | ~~פולג, קונטיקי, געש~~ **הוכרע 19/8/26: נשארים בחוץ.** הכלל של מדידה חיה כתנאי כניסה נשאר נקי. מי שרוצה אותם מוסיף אותם בעצמו דרך שלב 6 | ✅ |
| 3 | **שם ודומיין** | ⬜ `k8now` הוא שרת דיסקורד (512 חברים, קהילה — לא מוצר תחזית). ההמלצה: `kite-radar.pages.dev` — חינם, מיידי, ואפשר להצמיד דומיין אחר כך בלי לגעת בקוד |
| 4 | **הפעלת SW** — לא אומתה חיה | ⬜ לאמת בפריסה הראשונה: לפתוח באייפון, Share ← הוספה למסך הבית, ואז מצב טיסה |
| 5 | **`GITHUB.owner`** ב-`config.js` = `erezlazarof` (אומת מול api.github.com) | ⬜ הקישור ל-issue יעבוד רק אחרי שהריפו `kite-radar` ייווצר |

---

## מלכודות שכבר נשרפנו בהן — לא לחזור עליהן

1. **`time_obs` של השמ"ט הוא UTC+2 קבוע, בלי שעון קיץ.** פרשנות שגויה מזיזה כל קריאה בשעה. `WS` הוא מטר לשנייה ולא קשר.
2. **ערך של custom property נפתר באלמנט שבו הוא מוצהר**, לא באלמנט שבו הוא נצרך. `--ch-line: var(--lv)` על `:root` נפתר מול `--lv` שאינו קיים שם, והמטאוגרם צויר בלבן. הטוקנים חייבים לשבת על `.card` / `.detail-panel`.
3. **הפאנל אינו צאצא של הכרטיס** בעץ ה-DOM — הוא סמוך לו. הוא חייב לקבל את מחלקת הדרגה בעצמו.
4. **ציר זמן RTL נעשה במיפוי ה-x, לעולם לא ב-`transform: scaleX(-1)`.** טרנספורם מראה משקף גם את חצי כיוון הרוח — הסימן הכי בטיחותי במסך. יש בדיקה שנכשלת אם מישהו יוסיף `scaleX`.
5. **מילה לטינית בתוך עברית** — `heText()` ב-`card.js` עוטף כל מילה בנפרד, ולכן הוא נכון רק למילה בודדת. **כל צירוף לטיני רב-מילי חייב `ltr()`** — בלוק אחד. נמדד: `heText('TEL AVIV COAST')` נרנדר על המסך כ-"COAST AVIV TEL", כי שני רצפי LTR שנפגשים מעבר לרווח ניטרלי מתהפכים.
6. **`var()` בתוך presentation attribute של SVG לא נתמך.** ה-CSS הוא מה שקובע; המאפיין הוא רק גיבוי כשאין גיליון סגנון.
7. **סקריפט תיקון שקורא קובץ פעמיים וכותב פעם אחת** דורס את השינוי הראשון בשקט. לקובץ אחד — עריכה ישירה. תמיד `assert` לפני `replace`.
8. **שומר הסף** חוסם פקודות שמכילות `C:\Program Files` או רצף `\n\n` בגוף סקריפט. לנסח מחדש או לעבור לקובץ, לא להתאבק.
9. **הדפדפן המוטמע חוסם Service Workers** ולעיתים נתקע. אימות ויזואלי: Chrome headless → `--screenshot` (לא `--print-to-pdf`, שדוחף תוכן לעמוד הבא), **פרופיל טרי בכל ריצה**, ו-`--force-prefers-reduced-motion` כי אנימציות `infinite` חוסמות את תקציב הזמן הווירטואלי.
10. **אחרי `render()` הפניה לאלמנט מתיישנת.** לתשאל מחדש לפי `data-spot`, אחרת קוראים שלוש פעמים את אותו פאנל.
11. **`Number(null)` הוא 0, לא NaN.** נשרפנו בזה **פעמיים**: בטופס, שם שדה נ.צ. ריק נקרא כנקודה (0,0) במפרץ גינאה; וב-`decodeShare`, שם קישור שיתוף עם `d` חסר ייצר **ניצב חוף של 0° — צפון** ועבר את האימות. כל המרה של קלט חיצוני עוברת דרך המרה מפורשת שמחזירה NaN על `null`, `''` ו-boolean.
12. **`compassHe()` מחזיר תואר** ("מערבית") ומתאים לרוח בלבד. "החוף פונה למערבית" אינו עברית — לחוף יש `compassNounHe()`.
13. **תצוגה מקדימה מתחברת, לא מרימה תהליך.** `.claude/launch.json` נוצר כשריד גנרי שתבע **פורט 3000 — שבו יושב `erez-dashboard`** של ארז. ההגדרה עכשיו היא attach ל-`http://localhost:3300` בלי `runtimeExecutable`, כלומר `preview_start` נצמד לשרת של pm2 ולא מרים שני. מפת הפורטים ב-`ecosystem.config.cjs`: 3000 דשבורד · 3100 מרכז שליטה · 3200 דירה-רדאר · 3300 כאן.
14. **`200` לא אומר שזה השרת שלך.** `node dev-server.js` בתוך worktree נפל בשקט ל-EADDRINUSE — 3300 מוחזק בידי `kite-radar-dev` של pm2, שמשרת את **master** — ובאותו רגע `curl` החזיר 200, ואני בדקתי וצילמתי את הקוד של master בלי לדעת. הסימן הוודאי הוא לחפש בתוכן משהו שקיים רק בענף שלך (`curl -s … | grep add-spot`), לא קוד סטטוס. בענף אחר: פורט אחר, לפי חוק 9.
15. **צילום headless בחלון צר נראה כאילו הפריסה גולשת גם כשהיא לא.** למדוד `getBoundingClientRect` לפני שמתקנים CSS על סמך מה שנראה בתמונה.

---

## חוקי הברזל של המוצר

אלה לא העדפות עיצוב — הם קבועים במנוע ומכוסים בבדיקות:

1. **אין מסלול קוד מנתון ישן או חסר אל ירוק.**
2. **אפור (`unknown`) אינו אדום.** "לא יודעים" ≠ "יודעים שרע".
3. **`blocked` (אסור/מחוץ לעונה) אינו אדום.** ערבוב השניים מלמד להתעלם מאדום.
4. **רוח מהחוף החוצה היא אדום, ואי אפשר לעקוף אותה בניקוד.** החריג היחיד הוא `direction_overrides` — נתון ברג'יסטר, לא ענף בקוד.
5. **מהירות היא שער, לא רק רכיב.** כיוון מושלם לא הופך 10 קשר לרכיבים.
6. **פסק הדין אובייקטיבי.** משקל הגולש **אינו** נוגע בו — `speedScore` מקבל מהירות בלבד, ויש בדיקה שנכשלת אם מישהו יחזיר לתוכו פרמטר. המשקל חי בשכבת התכנון ("מה לקחת") בלבד.
7. **מדידה חיה לא מחליפה פסק דין.** פער של 5 קשר ומעלה מוריד דרגה ומרים דגל — כי מה שיש לנו הוא חוסר ידיעה, לא תשובה אחרת.
8. **null לעולם לא ממולא באפס.** אין גלים לאילת ולכנרת — וזה נאמר.
9. **דגלי סכנה מוצגים תמיד**, גם כשהמספר טוב.
10. **שכבת התכנון שותקת בכרטיס אדום.** "לא ללכת" ואחריו "מהעפיפונים שלך: 7 מטר" נקרא כעידוד.

---

## רישוי — לא לגעת בלי לקרוא

* **Open-Meteo** — CC BY 4.0, **ייחוס חובה**. בפוטר.
* **השירות המטאורולוגי** — הרישיון **מתיר** פרסום מחדש, ומחייב ייחוס גלוי + הצהרה שהשמ"ט אינו אחראי ואינו צד לפרסום. **סעיף 10 מחייב חוזית ניטור הפסקות בהזנה ומנגנון גיבוי** — לכן `feed.state` בכל תגובה, `Promise.allSettled`, ו-`stale-while-revalidate`. זה קוד, לא הערה.
* דגל כיבוי: `FEATURES.ims_live` ב-`config.js` — שורה אחת אם התנאים ישתנו.

---

## ממצאי סוכן הטלגרם — לקרוא לפני נגיעה ב-worker/

* **`<span dir="ltr">` שובר כל הודעת טלגרם.** מצב HTML של טלגרם מקבל רשימת תגיות סגורה שאינה כוללת `span[dir]` — הוא מחזיר 400 ומפיל את ההודעה כולה. `phrases.he.js` פולט בדיוק את זה, ולכן `toTelegramHtml()` ממיר לבידוד יוניקוד (U+2066/U+2069). **לא להעביר טקסט מהמנוע ישירות לטלגרם.**
* **`sent:` שומר `"<signature>@<epochMs>"`** ולא חתימה חשופה: רצפת 12 השעות צריכה זמן שליחה, ומפתח יומי לבדו לא מספק אותו.
* **הקרון קורא גם את המפתח של אתמול** — החתימה נושאת את יום *התחזית* והמפתח את יום *הריצה*, והתראה על מחר שנשלחה ב-18:00 יושבת במפתח של אתמול.
* **`0 4,10,15` הוא UTC ללא שעון קיץ** ⇒ 07/13/18 בקיץ, 06/12/17 בחורף. אם צריך קבוע — שני סטים או דילוג בקוד.
* **`wrangler` עדיין לא מותקן.** `npm install -g wrangler` הוא שלב אמיתי, לא פורמליות.
* **`FEATURES.telegram` ב-`config.js` הוא שריד** — לאתר אין ממשק התראות בכוונה, ואף אחד לא קורא אותו.

## חוב טכני ידוע

* **קומיטים מעורבבים.** קבצי ה-worker נסחפו לקומיטים של שלבים 2 ו-5 בגלל `git add -A` בזמן שסוכן כתב במקביל. התוכן שלם, אבל ההיסטוריה לא נקייה. בסשן הבא — לא לעשות `git add -A` כשסוכן רץ.

16. **מיכל flex ברוחב מלא סביב פקד קטן מייצר אזור נגיעה מת ומסוכן.** `#as-dial` היה 408 פיקסלים רוחב עם חוגה של 190 בתוכו — 102 פיקסלים של שטח מת בכל צד, שכל נגיעה בו **כתבה מחדש את ניצב החוף**. `pointerdown` על ה-host מחייב `e.target.closest('svg')`; הגרירה עצמה ממשיכה מחוץ לחוגה, כי זו גרירה.
17. **תוצר מחולל ששמור בגיט מתיישן בשקט.** `public/dev/map.html` נשאר על 42 ספוטים — כולל פולג, קונטיקי וגעש — אחרי שהרג'יסטר נגזם ל-26, ו-HANDOFF המשיך להפנות אליו כ"מפת הרג'יסטר". יש עכשיו בדיקה שמשווה את מספר הספוטים ואת השמות. לבנות מחדש: `node public/dev/build-map.js public/dev/map.html`.
18. **בדיקה שמאשרת מחדש את החוזה של הפונקציה אינה בדיקה.** `nearestStation(...).distance_km <= MAX_STATION_KM` עבר גם כשהקבוע שונה ל-90. סף נבדק משני צדיו, על נקודות שנבנות מהקבוע.
19. **בדיקה על החישוב אינה בדיקה על מה שמצויר.** ראה סעיף 2 בשלב 6 למעלה. אותו כלל תופס לכל רכיב SVG כאן.
20. **`state.expanded` מוצב רק בתוך `setExpanded()`.** השמה ישירה דילגה על `loadModels`, וספוט שנוסף מהטופס נפתח אוטומטית עם רצועת השוואה תקועה על "טוען השוואה…" לנצח. שער מבני בבדיקות.

## תחנות המועדונים (19/8, אחה"צ)

ארז צדק: יש מדי רוח של מועדונים, והם חוברו. שני סבבי מחקר רב-סוכני +
ביקורת אדוורסרית (9 ממצאים, כולם תוקנו ונעולים בבדיקות):

| ספוט | ראשי | גיבוי |
|---|---|---|
| ריף רף + אילת צפון | **Surf Center** (AWN, 21 מ' מהספוט, כל דקה, mph!) | IUI מטאו-טק |
| קריית ים + קריית חיים | **Surf Cycle/סורפו** (XML כל דקה, כבר בקשר!) | AFEQ שמ"ט |

**מלכודות שנקבעו בדם:**
* AWN משדר **mph** (×0.868976); סורפו כבר **בקשר** — המרה נוספת = חצי מהרוח.
* השעון של סורפו הוא **שעון ישראל עם שעון קיץ** — לא ה-UTC+2 הקבוע של
  השמ"ט. פורמט DD/MM. `israelOffsetMin` מקצץ לדקה עגולה לפני חישוב —
  אחרת שניות 31+ מזיזות את הקריאה דקה קדימה, ובגבול שעה לשעת תחזית שגויה.
* המהירות מ-AWN היא **ממוצע 10 דקות**, לא הרגעי (שקופץ 11→21 בין דקות).
* קריאה מתה לא מתפרסמת ב-clubs (ראה חוק 16); הבאנר הגלובלי "מדידה חיה
  לא זמינה" שמור ל**שמ"ט בלבד** — תחנת מועדון בודדת שנפלה מטופלת
  בשרשרת הנפילה של הכרטיס, לא בהכרזה על כולם.
* **אי-הסכמה בכיוון** היא דגל נפרד (`obs_dir_disagrees`): כשהמדידה על
  החוף בגזרת side_offshore/offshore והתחזית לא — צהוב + דגל. קרה חי
  באילת (354° נמדד מול 33° חזוי, מהירויות תואמות). אין מסלול שדרוג.

**מה נבדק ולא נכנס:** תשלום לא פותר כלום — iKitesurf/Windguru אוסרים
פרסום מחדש בכל המסלולים; תחנות ביתיות בגוש דן מראות רבע מהרוח האמיתית
(188 הושוו מול השמ"ט); בבת ים אין מדידה — galim.live היא תחזית
Open-Meteo מחופשת למדידה, ו-IRISHO15 עם שבשבת עקומה ב-+81°. הפירוט
המלא בפלטי הסבבים.

**שווה מייל בהמשך:** ims@ims.gov.il — ל-API של Envista יש ~85 תחנות
מול ה-65 שב-XML, בקצב דקה, תחת אותו רישיון שכבר מתיר לנו הכל.
