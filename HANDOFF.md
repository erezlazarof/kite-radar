# מסירה — רדאר קייט

**נכון ל-19/8/2026.** 192 בדיקות עוברות. עשרה קומיטים מקומיים. **שום דבר לא נדחף לגיט.**

---

## מה זה

אתר ציבורי חינמי בעברית: תחזית רוח **ומדידה חיה** לספוטי הקייטסרפינג בישראל, עם פסק דין "ללכת לים?" לכל חוף. ארז לזרוף וחבריו גולשי קייט.

התוכנית המלאה: `<the full plan, kept outside the repo>`
הכללים לסשן: `CLAUDE.md` בתיקייה הזו — **לקרוא לפני נגיעה בקוד.**

---

## הרצה — 30 שניות

**השרת כבר רץ** תחת pm2 בשם `kite-radar-dev` על פורט 3300, ומשרת את הריפו הראשי.
הוא **לא** רץ בתוך סשן: סשן שמחזיק תהליך חי נראה כאילו הוא עדיין עובד, ותהליך יתום
אחרי סגירת סשן תופס את הפורט וגורם ל-EADDRINUSE בסשן הבא. זה כבר קרה פעמיים.

```
pm2 list | Select-String kite       # מצב
pm2 logs kite-radar-dev --lines 50  # לוגים
pm2 restart kite-radar-dev          # אחרי שינוי בקוד השרת
```

⛔ **לא להריץ `node dev-server.js` בתוך סשן.** אם אתה בענף אחר (worktree) והשרת של
master לא מתאים — הרם משלך על פורט אחר: `PORT=3301 node dev-server.js`.

<http://127.0.0.1:3300> — האתר, כולל `/api/obs` החי.
`dev-server.js` מריץ את **הפונקציה האמיתית** מ-`functions/api/obs.js`, בלי wrangler ובלי פריסה. מה שנבדק כאן הוא מה שירוץ בענן.

| כתובת | מה |
|---|---|
| `/` | האפליקציה |
| `/dev/states.html` | **גלריית מצבים** — חמשת מצבי פסק הדין על נתונים סינתטיים. לפתוח אחרי כל שינוי ויזואלי; היא חשפה את רוב הבאגים |
| `/dev/map.html` | מפת הרג'יסטר. מחוללת ב-`node public/dev/build-map.js public/dev/map.html` |

**אם הפורט תפוס:** למצוא מי מחזיק אותו לפי PID, לא לפי שם —
`Get-NetTCPConnection -LocalPort 3300 -State Listen` ואז `Stop-Process -Id <pid>`.

`npm test` — 192 בדיקות. **להריץ לפני כל push.**

---

## מצב השלבים

| # | שלב | מצב |
|---|---|---|
| 0 | שלד ופריסה | ✅ |
| 1 | כרטיסי פסק דין | ✅ |
| 1b | מערכת עיצוב v2 | ✅ מטאוגרם בכרטיס, מצפן, פלטת ים, חמישה מצבים נבדלים מבנית |
| 1c | ניווט ורג'יסטר | ✅ **26 ספוטים**, צ'יפי אזור, קיבוץ עם כותרות דביקות, גיליון הגדרות |
| 2 | פאנל ומטאוגרם 72 שעות | ✅ עם סקראבר |
| 3 | השוואת מודלים | ✅ |
| 4 | מדידה חיה | ✅ |
| 5 | PWA | ✅ **⚠️ הפעלת ה-SW לא אומתה חיה** — ראה למטה |
| 6 | הוסף ספוט משלך | ⬜ **הבא בתור** |
| 7 | טלגרם | 🔄 קוד כתוב, 45 בדיקות · **ממתין לטוקן בוט ולפריסה ידנית של ארז** |
| 8 | עומק ימי (גלים) | ⬜ |

---

## הצעד הבא המיידי

**שלב 6 — "הוסף ספוט משלך".** התוכנית קובעת שלוש שכבות בלי מסד נתונים:

1. **מקומי** — פין על מפה או הדבקת נ.צ. + חוגה לגרירת `shore_normal_deg` → `localStorage['kite.userSpots']`.
2. **שיתוף** — סריאליזציה ל-base64url ב-hash: `#addspot=<b64>`. חברים מחליפים ספוטים בלי backend.
3. **קידום לליבה** — כפתור שפותח GitHub issue ממולא מראש; ארז מדביק ל-`spots.json` ודוחף. **גיט הוא מסד הנתונים, ושלב הבדיקה הוא הביקורת הבטיחותית.**

המנוע כבר תומך: ספוט עם `source: 'user'` **חסום על צהוב** ונושא דגל `unverified_spot`. יש בדיקה שאוכפת את זה.

⚠️ **הכלל שארז קבע ב-18/8: מדידה חיה היא תנאי הכרחי.** ספוט בלי תחנה בטווח 12 ק"מ או מקור חי חלופי לא נכנס לרג'יסטר. יש בדיקה שנכשלת אם מישהו יוסיף אחד. לספוט שהמשתמש מוסיף בעצמו הכלל לא חל — אבל הוא לעולם לא יגיע לירוק.

---

## מה שארז צריך לעשות בעצמו

**אני חסום מלהריץ `wrangler deploy`.** הארכיטקטורה תוכננה כך שהפקודה הזו נדרשת פעם אחת ואז כמעט לעולם לא.

1. **github.com → New repository → `kite-radar` → Public → בלי README.**
2. `git push -u origin main` — נפתח חלון התחברות פעם אחת בחיים.
3. **Cloudflare → Workers & Pages → Create → Pages → Connect to Git → `kite-radar`.**
   Framework preset **None** · Build command **ריק** · Output directory **`public`**.
   ⛔ **לא להפעיל Cloudflare Access.** ב-`erez-pm-dashboard` הוא כן מופעל ונעול למייל שלו — כאן ההפך. טעות שם מייצרת אתר שרק הוא יכול לפתוח, **ונראה שהוא עובד**.
4. טלגרם — לפי `worker/README.md`.

מכאן והלאה כל עדכון הוא `git add . ; git commit ; git push` וקלאודפלייר מעלה תוך ~30 שניות. **את זה אפשר לעשות לבד.**

---

## חובות פתוחים שדורשים הכרעה של ארז

| # | מה | למה זה תלוי בו |
|---|---|---|
| ~~1~~ | ~~אילת — איזה חוף?~~ **הוכרע 19/8/26: ארז גולש בריף רף.** הסקר צדק — ריף רף הוא חוף הקייט, והחוף הצפוני הוא חוף רחצה ומרינה. `eilat-reef-raf` סומן כמאושר-בעלים ודירוג הוודאות עלה ל-high | — |
| 2 | **פולג (21 ק"מ), קונטיקי (16), געש (20)** נפלו על כיסוי מדידה, לא על פרסום | הם ספוטים מוכרים. אם ירצה — יחזרו עם תווית "תחזית בלבד, אין תחנה קרובה" |
| 3 | **שם ודומיין** | `k8now` תפוס ע"י קהילת דיסקורד אירופאית |
| 4 | **הפעלת SW** — לא אומתה חיה | לאמת בפריסה הראשונה: לפתוח באייפון, Share → הוספה למסך הבית, ואז מצב טיסה |

---

## מלכודות שכבר נשרפנו בהן — לא לחזור עליהן

1. **`time_obs` של השמ"ט הוא UTC+2 קבוע, בלי שעון קיץ.** פרשנות שגויה מזיזה כל קריאה בשעה. `WS` הוא מטר לשנייה ולא קשר.
2. **ערך של custom property נפתר באלמנט שבו הוא מוצהר**, לא באלמנט שבו הוא נצרך. `--ch-line: var(--lv)` על `:root` נפתר מול `--lv` שאינו קיים שם, והמטאוגרם צויר בלבן. הטוקנים חייבים לשבת על `.card` / `.detail-panel`.
3. **הפאנל אינו צאצא של הכרטיס** בעץ ה-DOM — הוא סמוך לו. הוא חייב לקבל את מחלקת הדרגה בעצמו.
4. **ציר זמן RTL נעשה במיפוי ה-x, לעולם לא ב-`transform: scaleX(-1)`.** טרנספורם מראה משקף גם את חצי כיוון הרוח — הסימן הכי בטיחותי במסך. יש בדיקה שנכשלת אם מישהו יוסיף `scaleX`.
5. **מילה לטינית בתוך עברית** — `heText()` ב-`card.js`. **משפט אנגלי שלם** צריך בלוק `dir="ltr"` נפרד; העטיפה מילה-מילה מפרקת אותו.
6. **`var()` בתוך presentation attribute של SVG לא נתמך.** ה-CSS הוא מה שקובע; המאפיין הוא רק גיבוי כשאין גיליון סגנון.
7. **סקריפט תיקון שקורא קובץ פעמיים וכותב פעם אחת** דורס את השינוי הראשון בשקט. לקובץ אחד — עריכה ישירה. תמיד `assert` לפני `replace`.
8. **שומר הסף** חוסם פקודות שמכילות `C:\Program Files` או רצף `\n\n` בגוף סקריפט. לנסח מחדש או לעבור לקובץ, לא להתאבק.
9. **הדפדפן המוטמע חוסם Service Workers** ולעיתים נתקע. אימות ויזואלי: Chrome headless → `--screenshot` (לא `--print-to-pdf`, שדוחף תוכן לעמוד הבא), **פרופיל טרי בכל ריצה**, ו-`--force-prefers-reduced-motion` כי אנימציות `infinite` חוסמות את תקציב הזמן הווירטואלי.
10. **אחרי `render()` הפניה לאלמנט מתיישנת.** לתשאל מחדש לפי `data-spot`, אחרת קוראים שלוש פעמים את אותו פאנל.

---

## חוקי הברזל של המוצר

אלה לא העדפות עיצוב — הם קבועים במנוע ומכוסים בבדיקות:

1. **אין מסלול קוד מנתון ישן או חסר אל ירוק.**
2. **אפור (`unknown`) אינו אדום.** "לא יודעים" ≠ "יודעים שרע".
3. **`blocked` (אסור/מחוץ לעונה) אינו אדום.** ערבוב השניים מלמד להתעלם מאדום.
4. **רוח מהחוף החוצה היא אדום, ואי אפשר לעקוף אותה בניקוד.** החריג היחיד הוא `direction_overrides` — נתון ברג'יסטר, לא ענף בקוד.
5. **מהירות היא שער, לא רק רכיב.** כיוון מושלם לא הופך 10 קשר לרכיבים.
6. **פסק הדין אובייקטיבי.** משקל הגולש **אינו** נוגע בו — `speedScore` מקבל מהירות בלבד, ויש בדיקה שנכשלת אם מישהו יחזיר לתוכו פרמטר. המשקל חי בשכבת התכנון ("מה לקחת") בלבד.
7. **מדידה חיה לא מחליפה פסק דין.** פער של 5 קשר ומעלה מוריד דרגה ומרים דגל — כי מה שיש לנו הוא חוסר ידיעה, לא תשובה אחרת.
8. **null לעולם לא ממולא באפס.** אין גלים לאילת ולכנרת — וזה נאמר.
9. **דגלי סכנה מוצגים תמיד**, גם כשהמספר טוב.
10. **שכבת התכנון שותקת בכרטיס אדום.** "לא ללכת" ואחריו "מהעפיפונים שלך: 7 מטר" נקרא כעידוד.

---

## רישוי — לא לגעת בלי לקרוא

* **Open-Meteo** — CC BY 4.0, **ייחוס חובה**. בפוטר.
* **השירות המטאורולוגי** — הרישיון **מתיר** פרסום מחדש, ומחייב ייחוס גלוי + הצהרה שהשמ"ט אינו אחראי ואינו צד לפרסום. **סעיף 10 מחייב חוזית ניטור הפסקות בהזנה ומנגנון גיבוי** — לכן `feed.state` בכל תגובה, `Promise.allSettled`, ו-`stale-while-revalidate`. זה קוד, לא הערה.
* דגל כיבוי: `FEATURES.ims_live` ב-`config.js` — שורה אחת אם התנאים ישתנו.

---

## ממצאי סוכן הטלגרם — לקרוא לפני נגיעה ב-worker/

* **`<span dir="ltr">` שובר כל הודעת טלגרם.** מצב HTML של טלגרם מקבל רשימת תגיות סגורה שאינה כוללת `span[dir]` — הוא מחזיר 400 ומפיל את ההודעה כולה. `phrases.he.js` פולט בדיוק את זה, ולכן `toTelegramHtml()` ממיר לבידוד יוניקוד (U+2066/U+2069). **לא להעביר טקסט מהמנוע ישירות לטלגרם.**
* **`sent:` שומר `"<signature>@<epochMs>"`** ולא חתימה חשופה: רצפת 12 השעות צריכה זמן שליחה, ומפתח יומי לבדו לא מספק אותו.
* **הקרון קורא גם את המפתח של אתמול** — החתימה נושאת את יום *התחזית* והמפתח את יום *הריצה*, והתראה על מחר שנשלחה ב-18:00 יושבת במפתח של אתמול.
* **`0 4,10,15` הוא UTC ללא שעון קיץ** ⇒ 07/13/18 בקיץ, 06/12/17 בחורף. אם צריך קבוע — שני סטים או דילוג בקוד.
* **`wrangler` עדיין לא מותקן.** `npm install -g wrangler` הוא שלב אמיתי, לא פורמליות.
* **`FEATURES.telegram` ב-`config.js` הוא שריד** — לאתר אין ממשק התראות בכוונה, ואף אחד לא קורא אותו.

## חוב טכני ידוע

* **קומיטים מעורבבים.** קבצי ה-worker נסחפו לקומיטים של שלבים 2 ו-5 בגלל `git add -A` בזמן שסוכן כתב במקביל. התוכן שלם, אבל ההיסטוריה לא נקייה. בסשן הבא — לא לעשות `git add -A` כשסוכן רץ.
