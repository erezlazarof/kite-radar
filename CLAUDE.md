# רדאר קייט — הנחיות לסשן

## מה זה
אתר ציבורי חינמי: תחזית רוח לספוטי קייט בישראל + פסק דין "ללכת לים?" לכל חוף.
עברית, RTL, נייד. ארז וחבריו גולשי קייט.

## חוקי ברזל

1. **הקובץ `public/js/verdict/` הוא ליבת בטיחות.** שינוי שם דורש בדיקה. כל שינוי בניקוד מלווה בבדיקה חדשה ב-`test/verdict.test.js`. להריץ `npm test` לפני כל push.
2. **מודול `verdict/` חייב להישאר טהור** — בלי DOM, בלי fetch, בלי `Date.now()`. הזמן מוזרק. הוא רץ גם ב-Worker.
3. **חריגים פר-ספוט הם נתונים ב-`spots.json`, לא ענפים בקוד.** אם משהו נכון רק לאילת — הוא שדה ברג'יסטר.
4. **לא להמציא מספר שאין.** אין תחזית גלים לאילת ולכנרת. `null` נשאר `null` ומוצג כהיעדר.
5. **בלי build step.** ES modules ישירות. אם משהו דורש bundler — קודם לשאול למה.
6. **טווח מספרי בעברית חייב `<span dir="ltr">`.** "12–18" מתהפך בלי זה. לאמת במדידת `x`, לא בעין.
7. **`git push` הוא הפריסה.** אין פקודת deploy לאתר. `wrangler deploy` נוגע רק ל-`worker/` וארז מריץ אותו — Claude חסום.
8. **סודות רק ב-`worker/`, דרך `wrangler secret`.** לעולם לא בריפו, לא ב-Pages, לא בדפדפן.

## סטטוס שלבים

| שלב | מצב |
|---|---|
| 0 — שלד ופריסה | ✅ |
| 1 — כרטיסי פסק דין | ✅ 8 ספוטים, מנוע מלא, 37 בדיקות |
| 2 — פאנל ומטאוגרם | ⬜ |
| 3 — השוואת מודלים | ⬜ קוד המשיכה קיים ב-`sources/openmeteo.js`, `FEATURES.models=false` |
| 4 — מדידה חיה | ⬜ `functions/api/obs.js` |
| 5 — PWA | ⬜ |
| 6 — כל הספוטים + הוסף משלך | ⬜ |
| 7 — טלגרם | ⬜ |
| 8 — עומק ימי | ⬜ |

## התוכנית המלאה
`<the full plan, kept outside the repo>`

## מה כבר אומת בקריאה אמיתית לשרת
* Open-Meteo: כל הספוטים בקריאה אחת, CORS `*`, בלי מפתח, קשרים.
* אין תחזית גלים במפרץ אילת — `null` בכל המודלים.
* רישיון השמ"ט **מתיר** פרסום מחדש; מחייב ייחוס + **ניטור הפסקות ומנגנון גיבוי** (סעיף 10).
* תחנת השמ"ט `ELAT` מתה מבחינת רוח. אילת נשענת על scrape של Meteo-Tech — הספוט השביר.
* `time_obs` ב-XML של השמ"ט הוא **UTC+2 קבוע, בלי שעון קיץ**.
* בצת ואכזיב נופלים על אותו תא רשת — ברזולוציה של 7 ק"מ זו באמת אותה תחזית.
