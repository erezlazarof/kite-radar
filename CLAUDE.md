# רדאר קייט — הנחיות לסשן

## מה זה
אתר ציבורי חינמי: תחזית רוח לספוטי קייט בישראל + פסק דין "ללכת לים?" לכל חוף.
עברית, RTL, נייד. ארז וחבריו גולשי קייט.

## חוקי ברזל

1. **הקובץ `public/js/verdict/` הוא ליבת בטיחות.** שינוי שם דורש בדיקה. כל שינוי בניקוד מלווה בבדיקה חדשה ב-`test/verdict.test.js`. להריץ `npm test` לפני כל push.
2. **מודול `verdict/` חייב להישאר טהור** — בלי DOM, בלי fetch, בלי `Date.now()`. הזמן מוזרק. הוא רץ גם ב-Worker.
3. **חריגים פר-ספוט הם נתונים ב-`spots.json`, לא ענפים בקוד.** אם משהו נכון רק לאילת — הוא שדה ברג'יסטר.
4. **מדידה חיה היא תנאי כניסה לרג'יסטר** (החלטת ארז, 18/8). ספוט בלי תחנה בטווח 12 ק"מ או מקור חי חלופי — לא נכנס. `npm test` אוכף.
5. **מדידה חיה לא מחליפה פסק דין.** כשהתחנה חלוקה על המודל ב-5 קשר ומעלה, התוצאה היא הורדת דרגה ודגל — כי מה שיש לנו הוא חוסר ידיעה, לא תשובה אחרת.
6. **לא להמציא מספר שאין.** אין תחזית גלים לאילת ולכנרת. `null` נשאר `null` ומוצג כהיעדר.
7. **בלי build step.** ES modules ישירות. אם משהו דורש bundler — קודם לשאול למה.
8. **טווח מספרי בעברית חייב `<span dir="ltr">`.** "12–18" מתהפך בלי זה. לאמת במדידת `x`, לא בעין.
9. **גלריית המצבים** ב-`public/dev/states.html` מרנדרת את כל חמשת המצבים על נתונים סינתטיים. **לפתוח אותה אחרי כל שינוי ויזואלי** — היא חשפה כפילות צ'יפ, קו שטוח וקיר בולטים שהמסך החי לא הראה.
10. **`git push` הוא הפריסה.** אין פקודת deploy לאתר. `wrangler deploy` נוגע רק ל-`worker/` וארז מריץ אותו — Claude חסום.
11. **סודות רק ב-`worker/`, דרך `wrangler secret`.** לעולם לא בריפו, לא ב-Pages, לא בדפדפן.

## סטטוס שלבים

| שלב | מצב |
|---|---|
| 0 — שלד ופריסה | ✅ |
| 1 — כרטיסי פסק דין | ✅ 8 ספוטים, מנוע מלא |
| 1b — מערכת עיצוב v2 | ✅ פלטת ים, מטאוגרם 24 שעות, מצפן, רצועת תקציר |
| 1c — ניווט ורג'יסטר | ✅ **26 ספוטים** — נגזם ל-established בלבד, כולם עם מדידה חיה. היה 42 (~18 ספוטים בחמישה אזורים · צ'יפי אזור עם נקודת מצב · קיבוץ עם כותרות דביקות · גיליון הגדרות. **73 בדיקות** |
| 2 — פאנל ומטאוגרם | ✅ מטאוגרם 72 שעות עם סקראבר, מיפוי ציר משותף לציור ולמישוש |
| 3 — השוואת מודלים | ✅ רצועת פער, משיכה עצלה בפתיחת כרטיס, ICON מוחרג באילת עם הסבר |
| 4 — מדידה חיה | ✅ `/api/obs` חי: 65 תחנות שמ"ט + מצוף חדרה + תחנת IUI אילת · "נמדד עכשיו מול התחזית" בכל כרטיס · ניטור הפסקות לפי סעיף 10 ברישיון |
| 5 — PWA | ✅ manifest, sw עם שלוש אסטרטגיות מטמון, שורת עדכון, מצב אופליין. **⚠️ הפעלת ה-SW לא אומתה חיה** — הדפדפן המוטמע חוסם, ו-headless לא מסיים התקנה. לאמת בפריסה הראשונה |
| 6 — כל הספוטים + הוסף משלך | ⬜ |
| 7 — טלגרם | ✅ קוד מלא · 45 בדיקות · **ממתין להתקנת wrangler, טוקן בוט ופריסה ידנית** — `worker/README.md` |
| 8 — עומק ימי | ⬜ |

## מסירה
`HANDOFF.md` — מצב מלא, מלכודות, וחובות פתוחים. **לקרוא ראשון בסשן חדש.**

## התוכנית המלאה
`<the full plan, kept outside the repo>`

## מה כבר אומת בקריאה אמיתית לשרת
* Open-Meteo: כל הספוטים בקריאה אחת, CORS `*`, בלי מפתח, קשרים.
* אין תחזית גלים במפרץ אילת — `null` בכל המודלים.
* רישיון השמ"ט **מתיר** פרסום מחדש; מחייב ייחוס + **ניטור הפסקות ומנגנון גיבוי** (סעיף 10).
* תחנת השמ"ט `ELAT` מתה מבחינת רוח. אילת נשענת על scrape של Meteo-Tech — הספוט השביר.
* `time_obs` ב-XML של השמ"ט הוא **UTC+2 קבוע, בלי שעון קיץ**.
* בצת ואכזיב נופלים על אותו תא רשת — ברזולוציה של 7 ק"מ זו באמת אותה תחזית.
