node_modules/
.wrangler/

# ⛔ סודות. חוק ברזל 12: סודות רק ב-worker/ דרך wrangler secret, לעולם
# לא בריפו. הריפו הזה ציבורי — שורה שנמחקת כאן היא דליפה.
.dev.vars
.env
.env.*

*.tmp
*.tmp.*
.DS_Store
Thumbs.db

# מצב מקומי של Claude Code. ⚠️ הריפו ציבורי: .claude/worktrees/ שוקל
# 1.3MB של מצב גיט, ו-ims_tmp.xml הוא זבל מחקר של צד שלישי. רק קובץ
# ההפעלה נשמר — כדי שהסשן הבא לא ייצור מחדש קונפיג גנרי שתובע פורט 3000.
.claude/*
!.claude/launch.json

# פלט מחקר וקבצי עבודה שנסחפים ב-git add -A.
# ⚠️ קרה בפועל: hist.json — 115KB של נתוני צד שלישי — נכנס לקומיט
# כשסוכן מחקר כתב אותו לשורש הריפו.
hist.json
_verify.html
public/_verify.html
public/dev/_verify.html
